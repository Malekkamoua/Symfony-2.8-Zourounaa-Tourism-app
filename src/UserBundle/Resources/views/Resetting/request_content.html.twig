{% extends "NavigationBundle:Default:base.html.twig" %}
{% trans_default_domain 'FOSUserBundle' %}

{% block title %}Resetting password{% endblock %}

{% block body %}
<body id="login_bg">	
	<nav id="menu" class="fake_menu"></nav>
	 
	<div id="login">
		<aside>
			<figure>
				<a href="{{ path('navigation_home')}}"><img src="{{asset("img/logo.png")}}" width="350" height="150" alt=""
						class="logo_sticky logo"></a>
			</figure>
        <br><br><br>
   
    <form action="{{ path('fos_user_resetting_send_email') }}" method="POST" class="fos_user_resetting_request">
        <div>
            <div class="form-group">
                <label for="username">{{ 'resetting.request.username'|trans }}</label> <br>
            </div>

            <div class="form-group">
                <input type="text" id="username" name="username" required="required" />
            </div>
        </div>
        <div>
            <input type="submit" value="{{ 'resetting.request.submit'|trans }}" class="btn_1 rounded " />
        </div>
    			</form>
			<div class="copy">© 2019 {{website_name}}</div>
		</aside>
	</div>
    </body>
{% endblock %}

{% block javascripts %}
{% javascripts %} {{ parent() }} {% endjavascripts %}
{% endblock %}
